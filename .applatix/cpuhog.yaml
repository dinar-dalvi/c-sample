---
type: "service_template"
subtype: "container"
name: "cpuhog"
description: "a cpu hog"
container:
  resources:
    mem_mib: 1024
    cpu_cores: 1.5
  image: "ubuntu:latest"
  docker_options: ""
  command: sh -c 'sleep 300'
inputs:
  parameters:
    commit:
      default: "%%session.commit%%"
